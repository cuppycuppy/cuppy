<%= form_for(@recipe_situation) do |f| %>
  <% if @recipe_situation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_situation.errors.count, "error") %> prohibited this recipe_situation from being saved:</h2>

      <ul>
      <% @recipe_situation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br>
    <%= f.number_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :situation_id %><br>
    <%= f.number_field :situation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
